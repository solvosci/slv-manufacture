For those MRPs linked to projects, when account moves are generated, project
analytic account is automatically set.
Also adds a new check that identifies those journal items linked to MRP moves
that belong to direct child productions. This mark is set when main production
is marked as done.
It also adds a new check that identifies those journal items linked 
to MRP moves that has been unbuilt.
