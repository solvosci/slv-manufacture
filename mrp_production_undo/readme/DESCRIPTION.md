Enables undo (back to draft) a complete Production, updating stock inventory.

This is enabled only starting from a "done" state and for users that belong to
a specific security group. Anyway, this operation can be properly bypassed, if
such operation is needed within a server process.
