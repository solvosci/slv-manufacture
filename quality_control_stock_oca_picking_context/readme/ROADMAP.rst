* This it's intended to be added to main `quality_control_stock_oca`` addon
  as an improvement, and once done, this addon will become unnecessary.
* Current context is not fully completed, because `reference` field for the
  inspection is not currently filled.
