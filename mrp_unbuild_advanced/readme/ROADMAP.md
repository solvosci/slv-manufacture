* This addon depends on ``mrp_unbuild_bom_cust_qty`` due to tablet view, that
  explicity adapts it also for custom quantity views added by that addon.
  A bridge addon should be depeloped instead.
* Proper ``calendar`` view display name is lost, from v13. Restore it
* Uninstall hook for existing updated actions.
